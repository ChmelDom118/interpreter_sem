declare:
 function factorial(integer a) -> integer
  integer result = 0;
  if a > 1 then
   result = a * execute factorial(a - 1);
  elseif a == 2 then
   integer r = pi * pi / (execute pow(a) + c);
  elseif a <= 10 * 6 then
  else
   result = 1;
   boolean b = true;
  end;
  return result;
 end;
begin:
 for i = 1; i <= 10; i = i + 1; do
  integer f = execute factorial(i);
  execute print(f);
 end;
 while running do
 end;
 repeat integer a = 10; until a < 10 + b * execute c() end;
end;